// The app version the menu shows (bottom-left, muted). Bumped as part of each version's close-out
// commit — a stale value here is a release-discipline failure, which is exactly why it is one
// constant in one file rather than scattered.
export const LUNA_VERSION = 'v0.51.5';
